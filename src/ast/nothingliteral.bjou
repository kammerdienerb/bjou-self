# nothingliteral.bjou

type nothingliteral extends exprbase {
    proc init(this) {
        exprbase.init(this)
    }

    proc create() : nothingliteral {
        r := { nothingliteral: }
        r.init()

        return r
    }
}
