# boolliteral.bjou

type boolliteral extends exprbase {
    val : bool


    proc init(this, val : bool) {
        exprbase.init(this)
        this.val  = val
    }

    proc create(val : bool) : boolliteral {
        r := { boolliteral: }
        r.init(val)

        return r
    }
}
