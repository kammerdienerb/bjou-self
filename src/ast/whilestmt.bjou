type whilestmt extends astbase {
    cond  : astref
    stmts : astref[...]


    proc init(this) {
        astbase.init(this)
        this.stmts = [...astref]
    }

    proc create() : whilestmt {
        r := { whilestmt: }
        r.init()
        return r
    }
}
