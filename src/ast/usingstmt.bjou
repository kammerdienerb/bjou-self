# usingstmt.bjou

type usingstmt extends astbase {
    imprt : astref
    ident : string


    proc init(this) {
        astbase.init(this)
        this.ident = str()
    }

    proc create() : usingstmt {
        r := { usingstmt: }
        r.init()
        return r
    }
}
