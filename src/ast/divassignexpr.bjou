type divassignexpr extends binexprbase {
    proc init(this) {
        binexprbase.init(this)
    }

    proc create() : divassignexpr {
        r := { divassignexpr: }
        r.init()
        return r
    }
}
