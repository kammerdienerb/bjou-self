type plusassignexpr extends binexprbase {
    proc init(this) {
        binexprbase.init(this)
    }

    proc create() : plusassignexpr {
        r := { plusassignexpr: }
        r.init()
        return r
    }
}
