# notexpr.bjou

type notexpr extends exprbase {
    right : astref


    proc init(this) {
        exprbase.init(this)
    }

    proc create() : notexpr {
        r := { notexpr:  }
        r.init()
        return r
    }
}
