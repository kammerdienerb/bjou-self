# sizeofexpr.bjou

type sizeofexpr extends exprbase {
    type_decl : astref


    proc init(this) {
        exprbase.init(this)
    }

    proc create() : sizeofexpr {
        r := { sizeofexpr:  }
        r.init()
        return r
    }
}
