# newexpr.bjou

type newexpr extends exprbase {
    type_decl : astref


    proc init(this) {
        exprbase.init(this)
    }

    proc create() : newexpr {
        r := { newexpr:  }
        r.init()
        return r
    }
}
