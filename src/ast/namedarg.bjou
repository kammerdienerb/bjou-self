# namedarg.bjou

type namedarg extends exprbase {
    name : string
    expr : astref

    
    proc init(this) {
        exprbase.init(this)
    }

    proc create() : namedarg {
        r := { namedarg:  }
        r.init()
        return r
    }
}
