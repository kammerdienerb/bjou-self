type assignexpr extends binexprbase {
    proc init(this) {
        binexprbase.init(this)
    }

    proc create() : assignexpr {
        r := { assignexpr: }
        r.init()
        return r
    }
}
