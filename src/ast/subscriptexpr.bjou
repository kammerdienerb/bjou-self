type subscriptexpr extends binexprbase {
    proc init(this) {
        binexprbase.init(this)
    }

    proc create() : subscriptexpr {
        r := { subscriptexpr: }
        r.init()
        return r
    }
}
