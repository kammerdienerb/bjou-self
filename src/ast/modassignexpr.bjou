type modassignexpr extends binexprbase {
    proc init(this) {
        binexprbase.init(this)
    }

    proc create() : modassignexpr {
        r := { modassignexpr: }
        r.init()
        return r
    }
}
