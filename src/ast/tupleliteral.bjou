# tupleliteral.bjou

type tupleliteral extends exprbase {
    exprs : astref[...]


    proc init(this) {
        exprbase.init(this)
        this.exprs = [...astref]
    }

    proc create() : tupleliteral {
        r := { tupleliteral:  }
        r.init()
        return r
    }
}
