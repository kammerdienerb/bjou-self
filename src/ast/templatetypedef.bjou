# templatetypedef.bjou

type templatetypedef extends declaration {
    template_def : astref
    template     : astref


    proc init(this) {
        declaration.init(this)
    }

    proc create() : templatetypedef {
        r := { templatetypedef:  }
        r.init()
        return r
    }
}
