# templateinst.bjou

type templateinst extends astbase {
    type_params : astref[...]

    proc init(this) {
        astbase.init(this)
        this.type_params = [...astref]
    }

    proc create() : templateinst {
        r := { templateinst: }
        r.init()
        return r
    }
}
