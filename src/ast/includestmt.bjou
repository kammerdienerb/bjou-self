# includestmt.bjou

type includestmt extends astbase {
    path      : string
    container : (include_container ref | none)


    proc init(this) {
        astbase.init(this)
        this.path = str()
    }

    proc create() : includestmt {
        r := { includestmt: }
        r.init()
        return r
    }
}
